Upload index.html, styles.css, and trophy.png to the root of the existing GitHub repository. Replace the existing index.html and styles.css, then commit the changes. The App Store button currently uses href="#"; replace it with the final App Store URL when available.
